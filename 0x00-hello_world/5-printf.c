#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Always print with proper outcome
 *
 * Return: Always zero (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
